library(shiny)

ui <- fluidPage(
  BBPselectInput("ex",
                 "Example",
                 choices = c("a", "b", "c")
                 )
)

server <- function(input, output, session) {

}

shinyApp(ui, server)
